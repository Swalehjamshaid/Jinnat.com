# =========================================================
# FF Tech Website Audit SaaS – requirements.txt
# Comprehensive & future-proof version
# Covers all current + enterprise PDF + AI + PPT + charts + QR
# Railway / Docker / SSL-safe
# Updated: February 11, 2026
# =========================================================

# ───────────────────────────────────────────────
# Core FastAPI Backend
# ───────────────────────────────────────────────
fastapi==0.115.6
uvicorn[standard]==0.32.1
gunicorn==23.0.0

# ───────────────────────────────────────────────
# Templates, Static Files & Uploads
# ───────────────────────────────────────────────
jinja2==3.1.5
python-multipart==0.0.20

# ───────────────────────────────────────────────
# WebSocket Support
# ───────────────────────────────────────────────
websockets==14.1

# ───────────────────────────────────────────────
# Data Validation & Configuration
# ───────────────────────────────────────────────
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# ───────────────────────────────────────────────
# HTTP Clients & SSL / Security
# ───────────────────────────────────────────────
requests==2.32.3
httpx==0.28.1
urllib3==2.3.0
certifi==2024.12.14
cryptography==42.0.8

# ───────────────────────────────────────────────
# HTML Parsing & Website Analysis
# ───────────────────────────────────────────────
beautifulsoup4==4.12.3
lxml==5.3.0

# ───────────────────────────────────────────────
# High-performance JSON handling
# ───────────────────────────────────────────────
orjson==3.10.15
ujson==5.10.0

# ───────────────────────────────────────────────
# PDF Generation – Core & Professional Quality
# ───────────────────────────────────────────────
reportlab>=4.2.0
Pillow>=10.0.0
fonttools>=4.53.0

# ───────────────────────────────────────────────
# PDF Charts & Visualization
# ───────────────────────────────────────────────
matplotlib>=3.8.0

# ───────────────────────────────────────────────
# QR Codes & PowerPoint Export
# ───────────────────────────────────────────────
qrcode[pil]>=7.4.2
python-pptx>=1.0.2
openpyxl>=3.1.5                  # Optional Excel export

# ───────────────────────────────────────────────
# Optional Advanced PDF / HTML → PDF
# ───────────────────────────────────────────────
# weasyprint==63.0                 # Uncomment only if switching from ReportLab

# ───────────────────────────────────────────────
# Optional – Debugging & Performance
# ───────────────────────────────────────────────
# structlog==24.4.0                # Optional structured logging

# ───────────────────────────────────────────────
# RAILWAY / DOCKER NOTES
# ───────────────────────────────────────────────
# For ReportLab + DejaVuSans font:
# RUN apt-get update && apt-get install -y fonts-dejavu-core fontconfig
#
# For WeasyPrint:
# RUN apt-get update && apt-get install -y \
#     libcairo2 libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf2.0-0 \
#     libffi-dev shared-mime-info
