# ───────────────────────────────────────────────
# Core Web Framework & Server (FastAPI ecosystem)
# ───────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==23.0.0
python-multipart==0.0.9
websockets==13.1
starlette==0.40.0                  # usually pulled by fastapi, but explicit sometimes helps
anyio==4.6.2                       # async utilities

# ───────────────────────────────────────────────
# Data Validation, Settings, Environment
# ───────────────────────────────────────────────
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
python-jose[cryptography]==3.3.0   # JWT if you add authentication later
passlib[bcrypt]==1.7.4             # password hashing

# ───────────────────────────────────────────────
# HTTP Clients, Utilities, Security
# ───────────────────────────────────────────────
requests==2.32.3
httpx==0.27.2
urllib3==2.2.3
certifi==2024.8.30
cryptography==43.0.1
aiohttp==3.10.10                   # async HTTP client (useful for concurrent fetches)

# ───────────────────────────────────────────────
# HTML / XML / Parsing / Scraping
# ───────────────────────────────────────────────
beautifulsoup4==4.12.3
lxml==5.3.0
html5lib==1.1
cssutils==2.5.1
playwright==1.48.0                 # optional: full browser automation if static scraping fails
# playwright install --with-deps chromium   (run this once locally or in build)

# ───────────────────────────────────────────────
# Fast & High-performance JSON / Serialization
# ───────────────────────────────────────────────
orjson==3.10.7
ujson==5.10.0

# ───────────────────────────────────────────────
# PDF Generation – Core & Extended
# ───────────────────────────────────────────────
reportlab==4.2.2
Pillow==10.4.0
fonttools==4.53.1
weasyprint==62.3                   # alternative PDF engine (CSS → PDF, very powerful)
pypdf==5.1.0                       # PDF manipulation / merging / splitting
pdfplumber==0.11.4                 # PDF text & table extraction
PyMuPDF==1.24.10                   # fitz – very fast PDF rendering & text extraction

# ───────────────────────────────────────────────
# Jinja & Templating (useful for dynamic PDF/email/report content)
# ───────────────────────────────────────────────
jinja2==3.1.4
jinja2-time==0.2.0

# ───────────────────────────────────────────────
# Charting, Visualization, Data Processing
# ───────────────────────────────────────────────
matplotlib==3.9.2
numpy==2.1.1
pandas==2.2.3
contourpy==1.3.0
cycler==0.12.1
kiwisolver==1.4.5
pyparsing==3.1.4
python-dateutil==2.9.0.post0
seaborn==0.13.2                    # nicer statistical plots on top of matplotlib

# ───────────────────────────────────────────────
# Office Documents & Spreadsheets
# ───────────────────────────────────────────────
openpyxl==3.1.5
python-pptx==0.6.23
qrcode[pil]==7.4.2
xlrd==2.0.1                        # legacy .xls reading if needed
xlsxwriter==3.2.0                  # alternative Excel writer

# ───────────────────────────────────────────────
# Logging, Monitoring, Performance
# ───────────────────────────────────────────────
structlog==24.4.0                  # structured logging
sentry-sdk[fastapi]==2.19.0        # error tracking (add DSN in env)
prometheus-client==0.21.0          # metrics if you expose /metrics endpoint

# ───────────────────────────────────────────────
# Testing & Development helpers (optional but very useful)
# ───────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx[http2]==0.27.2               # for testing
asgi-lifespan==2.1.0               # lifespan testing

# ───────────────────────────────────────────────
# Security & Rate Limiting (common additions)
# ───────────────────────────────────────────────
slowapi==0.1.9                     # rate limiting
python-dotenv==1.0.1               # already have, but kept for clarity
