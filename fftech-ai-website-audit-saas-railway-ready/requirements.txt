# =========================================================
# FF Tech Website Audit SaaS – requirements.txt
# Railway / Docker / SSL-safe / Full PDF management
# Updated: February 10, 2026
# =========================================================

# Core FastAPI + Server
fastapi==0.115.6
uvicorn[standard]==0.32.1
gunicorn==23.0.0

# Templates & Form handling
jinja2==3.1.5
python-multipart==0.0.20

# WebSocket support
websockets==14.1

# Validation & Configuration
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1

# HTTP Clients + SSL fixes (critical)
requests==2.32.3
httpx==0.28.1
urllib3==2.3.0
certifi==2024.12.14          # Fixes CERTIFICATE_VERIFY_FAILED
cryptography==42.0.8         # Modern TLS support

# HTML Parsing & Analysis (used in runner.py)
beautifulsoup4==4.12.3
lxml==5.3.0

# Fast JSON handling (performance boost)
orjson==3.10.15
ujson==5.10.0

# ───────────────────────────────────────────────
# PDF Generation & Management (core + advanced)
# ───────────────────────────────────────────────
reportlab==4.2.5                 # Classic PDF generation (your current library)
weasyprint==63.0                 # Modern HTML/CSS → PDF (best for professional reports)
pypdf==5.1.0                     # PDF manipulation (merge, split, add pages, metadata)
pillow==10.4.0                   # Image processing (logos, charts, thumbnails in PDFs)

# Optional PDF tools (uncomment only if you need them)
# pdfkit==1.0.0                  # wkhtmltopdf wrapper (alternative PDF engine)
# PyPDF2==3.0.1                  # Older PDF library (if you have legacy code)
# xhtml2pdf==0.2.16              # Another HTML-to-PDF option

# Optional extras (uncomment if used)
# qrcode==7.4.2                  # QR codes in reports
# python-pptx==1.0.2             # PowerPoint export
# openpyxl==3.1.5                # Excel export
