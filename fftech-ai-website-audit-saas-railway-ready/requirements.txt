# =========================================================
# FF Tech - Comprehensive Website Audit SaaS Requirements
# =========================================================
# Notes:
# - Keep versions pinned for stable Railway/Docker builds.
# - Optional sections can be removed if not used to reduce size.
# - Some tools (Playwright) need OS deps and an install step.
# =========================================================


# ---------------------------------------------------------
# 1) CORE WEB FRAMEWORK / SERVER
# ---------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.32.1
gunicorn==23.0.0
jinja2==3.1.5
python-multipart==0.0.20
websockets==14.1
python-dotenv==1.0.1

# Optional server improvements
orjson==3.10.15
ujson==5.10.0


# ---------------------------------------------------------
# 2) DATA VALIDATION / SETTINGS
# ---------------------------------------------------------
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0
validators==0.34.0


# ---------------------------------------------------------
# 3) HTTP CLIENTS / NETWORKING
# ---------------------------------------------------------
httpx==0.28.1
requests==2.32.3
urllib3==2.3.0
aiohttp==3.10.11
certifi==2024.12.14
idna==3.10

# DNS / URL parsing helpers (optional)
tldextract==5.1.2
yarl==1.18.3


# ---------------------------------------------------------
# 4) HTML PARSING / SCRAPING / CONTENT ANALYSIS
# ---------------------------------------------------------
beautifulsoup4==4.12.3
lxml==5.3.0
selectolax==0.3.25

# Robots / sitemap parsing
robotexclusionrulesparser==1.7.1
xmltodict==0.13.0


# ---------------------------------------------------------
# 5) SEO / STRUCTURED DATA / METADATA ANALYSIS (Optional but great)
# ---------------------------------------------------------
extruct==0.16.0
w3lib==2.3.1
python-slugify==8.0.4


# ---------------------------------------------------------
# 6) SECURITY AUDIT / TLS / HEADERS / CERT CHECKS
# ---------------------------------------------------------
cryptography==44.0.0
pyOpenSSL==25.0.0
certvalidator==0.11.1

# JWT/auth helpers (if you have accounts)
PyJWT==2.10.1
itsdangerous==2.2.0
passlib[bcrypt]==1.7.4


# ---------------------------------------------------------
# 7) DATABASE / ORM (Optional for SaaS persistence)
# ---------------------------------------------------------
sqlalchemy==2.0.36
psycopg2-binary==2.9.10

# Optional: async postgres driver (recommended if you go async DB)
asyncpg==0.30.0
alembic==1.14.1


# ---------------------------------------------------------
# 8) CACHING / RATE LIMIT / TASK QUEUE (Optional but strong)
# ---------------------------------------------------------
redis==5.2.1
cachetools==5.5.1

# Task queues (choose one; comment out if not needed)
celery==5.4.0
rq==2.1.0


# ---------------------------------------------------------
# 9) PERFORMANCE TESTING / TIMING / TRACING
# ---------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.2

# Optional: system/process metrics
psutil==6.1.1

# Optional: tracing/telemetry
opentelemetry-api==1.29.0
opentelemetry-sdk==1.29.0
opentelemetry-instrumentation-fastapi==0.50b0
opentelemetry-instrumentation-httpx==0.50b0


# ---------------------------------------------------------
# 10) ACCESSIBILITY AUDIT (Optional)
# ---------------------------------------------------------
# For a11y rules you can implement custom checks;
# these libs help parse & validate content.
html5lib==1.1


# ---------------------------------------------------------
# 11) TECHNOLOGY FINGERPRINTING (Optional)
# ---------------------------------------------------------
builtwith==1.3.4
python-Wappalyzer==0.3.1


# ---------------------------------------------------------
# 12) SCREENSHOT / RENDERING / JS-HEAVY SITES (Optional)
# ---------------------------------------------------------
# PLAYWRIGHT: best for screenshots + JS rendering.
# Requires OS deps + install step: `playwright install --with-deps`
playwright==1.49.1

# Selenium alternative (heavier; usually not needed if Playwright exists)
selenium==4.27.1

# Image processing
pillow==10.4.0


# ---------------------------------------------------------
# 13) REPORTING: PDF / XLSX / PPTX / CHARTS (World class output)
# ---------------------------------------------------------
reportlab==4.2.5
python-pptx==1.0.2
openpyxl==3.1.5

# Data / charts
pandas==2.2.3
numpy==2.2.0
matplotlib==3.9.3

# Optional: nicer plotting
seaborn==0.13.2

# Optional: HTML->PDF engine (alternative to ReportLab)
# WeasyPrint can make stunning PDF from HTML/CSS, but needs system libs.
weasyprint==63.0

# Optional: PDF read/merge tools (useful for packaging)
pypdf==5.1.0


# ---------------------------------------------------------
# 14) AI / LLM SERVICES (Optional)
# ---------------------------------------------------------
google-generativeai==0.8.3

# Optional: openai client (if you add it later)
openai==1.59.7


# ---------------------------------------------------------
# 15) SCHEDULING / AUTOMATION
# ---------------------------------------------------------
APScheduler==3.11.0
tqdm==4.66.1


# ---------------------------------------------------------
# 16) LOGGING / STRUCTURED LOGS / ERROR REPORTING (Optional)
# ---------------------------------------------------------
loguru==0.7.3
sentry-sdk==2.19.2


# ---------------------------------------------------------
# 17) SERVER HARDENING / SECURITY (Optional)
# ---------------------------------------------------------
python-dateutil==2.9.0.post0
tenacity==9.0.0


# ---------------------------------------------------------
# 18) DEV / QUALITY TOOLS (Optional - remove in prod if desired)
# ---------------------------------------------------------
black==24.10.0
ruff==0.8.4
mypy==1.14.0
